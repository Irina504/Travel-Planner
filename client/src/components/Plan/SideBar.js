import React from 'react'
import CustomPicker from "../Calendar/CustomPicker"

const SideBar = ({startDate, endDate}) => {
    return (
        <div>
            <p>{startDate}</p>
        </div>
    )
}

export default SideBar
